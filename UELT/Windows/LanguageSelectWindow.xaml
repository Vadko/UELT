<Window x:Class="UELT.LanguageSelectWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Фільтрування" Width="400" SizeToContent="Height" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Виберіть бажану мову/властивість для відображення:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="1" x:Name="LanguageComboBox" MinHeight="25" Margin="0,0,0,8"/>
        <CheckBox Grid.Row="2" x:Name="UseForAllCheckBox" Content="Застосувати для наступних файлів такого типу" Margin="0,0,0,12" Visibility="Collapsed"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Усі мови/властивості" Width="135" MinHeight="25" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Лише вибране" Width="100" MinHeight="25" IsDefault="True" Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>